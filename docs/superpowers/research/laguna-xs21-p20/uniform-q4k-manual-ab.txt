# 2026-07-27 manual resident-vs-streamed comparison for the no-imatrix probe.
# The stock xs21_stream_ab.sh harness cannot run this artifact because each
# resident invocation exits 1 after early EOS.  This transcript is evidence
# of only the degenerate early-EOS comparison; it is not a replacement gate.
#
# Resident and streamed commands both used:
#   -m /Users/pauleveritt/projects/ds4/gguf/laguna-xs-2.1-uniform-routed-q4k-probe.gguf
#   -c 8192 -n 128 --nothink --temp 0
# Streamed additionally used: --ssd-streaming --ssd-streaming-cache-experts 800
MATCH exit=1 prompt=def fizzbuzz(n): output=To
MATCH exit=1 prompt=<html><head><title> output=It
MATCH exit=1 prompt=Explain HTTP caching briefly. output=HTTP
MATCH exit=1 prompt=import asyncio output=To
